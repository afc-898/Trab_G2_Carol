<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.antonio.starwarsapiclient">

    <application
        android:name=".StarWarsApiClientApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.search.PeopleSearchActivity"/>
        <activity android:name=".ui.activities.search.PlanetsSearchActivity"/>
        <activity android:name=".ui.activities.search.FilmsSearchActivity"/>
        <activity android:name=".ui.activities.details.PeopleDetailActivity"/>
        <activity android:name=".ui.activities.details.FilmDetailActivity"/>
        <activity android:name=".ui.activities.details.PlanetDetailActivity"/>
    </application>

</manifest>